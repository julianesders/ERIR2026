# ERIR2026 — Python dependencies
#
# Install into a project venv:
#   python -m venv .venv
#   source .venv/bin/activate      # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
# R packages: see README.md

# ── Core pipeline (cleaning + merging + spatial weights) ──────────────────────
pandas==3.0.0
numpy==2.4.2
openpyxl==3.1.5          # Excel backend for pandas (INKAR / KBA / crosswalk sources)
geopandas==1.1.3          # 04_clean_ladestationen_ags8.py, 01_spatial_weights_ags8.py
libpysal==4.14.1          # 01_spatial_weights_ags8.py (queen contiguity weights)
scipy==1.17.0             # 01_spatial_weights_ags8.py (sparse matrix ops)
requests==2.32.5          # 00_scraping/ web scrapers
beautifulsoup4==4.14.3    # 00_scraping/ HTML parsing

# ── One-time entity matching (02_merging/01_match_emk_ags.py only) ───────────
# Pulls torch + transformers (~2 GB). Only needed for the initial AGS–EMK match.
# Skip if the matched intermediate file already exists.
# linktransformer==0.1.18
