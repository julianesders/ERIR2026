# Copy this file to config.py and set BASE_PATH for your machine.
# config.py is gitignored — each machine keeps its own copy.
#
# To run scripts individually (outside main.py), add wd/ to PYTHONPATH first:
#   export PYTHONPATH=/path/to/ERIR2026/wd:$PYTHONPATH   # macOS/Linux
#   $env:PYTHONPATH = "C:\path\to\ERIR2026\wd"            # Windows PowerShell

BASE_PATH = "/path/to/ERIR2026/wd"
